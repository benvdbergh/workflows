# Workflows repository — planning manifest
#
# Backlog SSOT: Linear (delivery_tracker: linear). Generic project-planning
# defaults (Epics/ and Stories/ markdown) do NOT apply here.
#
# Full policy: .claude/skills/wf-plan/references/workflows-linear-backlog-override.md
# Global skill: project-planning (references/linear-adoption.md when delivery_tracker: linear)

version: 1

delivery_tracker: linear

linear:
  project_name: workflows
  project_id: 6196e3df-0c5a-4bb3-92fd-c537255d8ac0
  project_url: https://linear.app/ben-van-den-bergh/project/workflows-a5eb475ff80e/overview
  team_key: BEN
  mcp_server: plugin-linear-linear

# Requirements and contract docs stay in-repo (RFC, poc-scope, ADRs).
source_globs:
  - ROADMAP.md
  - "docs/**/*.md"
  - "docs/architecture/adr/**/*.md"

# Community code intake (bugs, security, small docs PRs) remains on GitHub — not the planning backlog.
github:
  repo: benvdbergh/workflows
  role: community_intake_and_prs

# Example Linear MCP usage (Cursor: plugin-linear-linear; authenticate with mcp_auth if needed).
# Tool schemas: Cursor MCP descriptors for plugin-linear-linear.
#
# Inspect project:
#   get_project — query "workflows"
#   list_milestones — project "workflows"
#   list_issues — project "workflows"
#
# Read/update backlog narrative:
#   get_issue — id or identifier (e.g. BEN-123)
#   save_issue — create or update description, milestone, labels, state
#
# Epic → milestone; story → issue; optional task → sub-issue (see linear-tooling-guide.md).
#
# Planning process: global project-planning skill + wf-plan/wf-design/wf-execute repo skills.
